# Patch files must stay byte-exact; never apply line-ending conversion.
*.patch -text
# Batch files are stored with CRLF so every download path (clone, raw, ZIP)
# yields a runnable file on Windows.
*.bat -text
